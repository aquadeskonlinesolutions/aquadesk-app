import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function RegisterPage() {
  return (
    <div className="min-h-screen bg-off-white p-8 max-w-2xl mx-auto">
      <PagePlaceholder
        title="Diver Registration"
        description="Public link shared with divers before arrival — waiver, medical questions, equipment preferences."
      />
    </div>
  );
}
