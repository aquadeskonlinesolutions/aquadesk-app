import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function SettingsPricingPage() {
  return (
    <PagePlaceholder
      title="Pricing & Rates"
      description="Course rates, equipment rental rates, packages, tiers, surcharges, exchange rates, government fees."
    />
  );
}
