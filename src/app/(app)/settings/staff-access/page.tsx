import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function SettingsStaffAccessPage() {
  return (
    <PagePlaceholder
      title="Staff Access"
      description="Manage login accounts, roles, and revenue visibility."
    />
  );
}
