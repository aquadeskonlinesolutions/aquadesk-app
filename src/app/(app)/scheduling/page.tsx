import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function SchedulingPage() {
  return (
    <PagePlaceholder
      title="Scheduling"
      description="Trip/schedule builder, boat and staff assignment."
    />
  );
}
