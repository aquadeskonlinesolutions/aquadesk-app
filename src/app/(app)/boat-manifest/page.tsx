import { PagePlaceholder } from "@/components/PagePlaceholder";

export default function BoatManifestPage() {
  return (
    <PagePlaceholder
      title="Boat Manifest"
      description="Per-trip manifest, offline-cacheable."
    />
  );
}
